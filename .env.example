# Ticketmaster API (Discovery API uses Consumer Key as API key)
# Get keys from https://developer.ticketmaster.com/ (Keys tab for your app)
TICKETMASTER_API_KEY=your_consumer_key_here
# Optional, for OAuth / Partner API only:
# TICKETMASTER_CONSUMER_SECRET=your_consumer_secret_here
